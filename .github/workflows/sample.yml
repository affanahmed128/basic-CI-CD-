name: Print Message

on: 
  push:
    branches: 
      - master
      
jobs: 
  print-message:
    runs-on: ubuntu-latest
    steps:
      - name: print message
        run: echo "new push message is added on the main branch"
