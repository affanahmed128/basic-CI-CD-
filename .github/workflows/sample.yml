name: Print Message

on: 
  push:
    branches: 
      - main
      
jobs: 
  print-message:
    runs-on: ubuntu-latest
    steps:
      - name: print message
        run: echo "new push message is added on the main branch"
